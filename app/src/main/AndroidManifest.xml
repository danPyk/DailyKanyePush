<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kanyenotifications">

    <application
        android:allowBackup="false"
        android:versionCode="26"
        android:versionName ="26"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/kanye_face"
        android:supportsRtl="false"
        android:theme="@style/Theme.DailyKanyePush">
        <activity
            android:name="com.example.kanyenotifications.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.DailyKanyePush.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name=".MainActivity" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>
        </activity>


        <service
            android:name="com.example.kanyenotifications.notifications.FirebaseService"
            android:exported="false">
            <intent-filter>
                <action android:name="com.google.firebase.MESSAGING_EVENT" />
            </intent-filter>
        </service>
        <!-- [START fcm_default_icon] -->
        <!--
 Set custom default icon. This is used when no icon is set for incoming notification messages.
             See README(https://goo.gl/l4GJaQ) for more.
        -->
    </application>

</manifest>